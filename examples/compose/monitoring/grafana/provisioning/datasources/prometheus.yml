# Grafana datasource provisioning — points the bundled Grafana at the
# prometheus service in this stack. Auto-loaded on Grafana startup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
